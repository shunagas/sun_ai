# ローカル限定のMCP設定（Playwright等）
.mcp.json

# 経理帳簿（実データを含むため、テンプレートを生成するスクリプトのみコミットし本体はコミットしない）
sun_accounting/経理帳簿.xlsx

# 作業中のスクリーンショット・Playwright一時ファイル（22時の夜間クリーンアップで.trash/へ退避）
/*.png
.playwright-mcp/

# 夜間クリーンアップの退避先（削除はせずここに移動するだけ。中身は本人が確認の上、手動で削除する）
.trash/

# 動画撮影素材（生データ）。容量が大きいためgit管理対象外。編集済みの最終成果物のみコミットする
*.MOV
*.mov
/羽田センチュリオン/
/香港ラウンジホップ/

# API認証情報（OAuthクライアントシークレット・リフレッシュトークン等）。絶対にコミットしない
.secrets/
**/.secrets/
*client_secret*.json
*_token.json
